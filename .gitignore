__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Never commit: contains access and refresh tokens in clear text.
tokens.json
*.tokens.json
.env

# The real deployment file, which holds your client secret and shared token.
# Commit docker-compose.example.yaml instead.
docker-compose.yaml
docker-compose.yml
data/
